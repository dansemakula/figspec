% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme.R
\name{theme_journal}
\alias{theme_journal}
\title{A ggplot2 theme that satisfies a journal's typography rules}
\usage{
theme_journal(
  journal,
  base = NULL,
  style = NULL,
  base_size = NULL,
  base_family = NULL
)
}
\arguments{
\item{journal}{Registry id, for example \code{"plos_one"}.}

\item{base}{A ggplot2 theme to build on. Defaults to \code{\link[ggplot2:theme_bw]{ggplot2::theme_bw()}}.}

\item{style}{A house style to apply: a name registered with
\code{\link[=register_house_style]{register_house_style()}}, a ggplot2 theme, or a function returning one.
Styles are applied underneath the journal's requirements and can never
override them.}

\item{base_size}{Base type size in points. Defaults to the journal's stated
minimum, or 9 pt when the journal states none.}

\item{base_family}{Font family. Defaults to the graphics device's own font.
The journal's named font is \emph{not} forced here, because a family the
current device cannot resolve makes the plot fail to render at all.
\code{\link[=ggsave_journal]{ggsave_journal()}} applies the journal's font at save time, where the
device is known. Pass a family explicitly to override.}
}
\value{
A ggplot2 theme object.
}
\description{
Journals set a floor on type size, and often name the fonts they accept.
\code{theme_journal()} takes any base theme and enforces those constraints on top
of it, rather than imposing a look of its own. Every text element is set to
at least the journal's minimum size, so nothing falls below the floor
because of a relative sizing rule in the base theme.
}
\details{
This only holds if the figure is saved at the journal's stated width, since
type size is fixed in points but a figure scaled down after the fact takes
its text with it. \code{\link[=ggsave_journal]{ggsave_journal()}} saves at the right width for you.
}
\examples{
library(ggplot2)
p <- ggplot(mtcars, aes(wt, mpg)) +
  geom_point() +
  theme_journal("plos_one")
}
