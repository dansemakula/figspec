% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save.R
\name{ggsave_journal}
\alias{ggsave_journal}
\title{Save a figure at a journal's required size and resolution}
\usage{
ggsave_journal(
  filename,
  plot = ggplot2::last_plot(),
  journal,
  column = c("single", "onehalf", "double"),
  height = NULL,
  units = c("mm", "cm", "in"),
  dpi = NULL,
  check = TRUE,
  ...
)
}
\arguments{
\item{filename}{Output path. The extension selects the format. If it has no
extension, the journal's first accepted format is used.}

\item{plot}{Plot to save. Defaults to the last plot displayed.}

\item{journal}{Registry id, for example \code{"cell_press"}.}

\item{column}{Which column width to size to.}

\item{height}{Height. Defaults to three quarters of the width, which is a
convenience, not a journal requirement.}

\item{units}{Units for \code{height}.}

\item{dpi}{Resolution. Defaults to the journal's stated minimum, or 300
when it states none.}

\item{check}{Whether to check the result and report failures as a warning.}

\item{...}{Passed to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}
}
\value{
The path to the written file, invisibly.
}
\description{
Works like \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}, but takes the width from the journal's
stated column width and the resolution from its stated minimum, so the
saved file is the size the journal asks for rather than a size that will be
rescaled later. Rescaling is what silently pushes type below the minimum.
}
\examples{
library(ggplot2)
p <- ggplot(mtcars, aes(wt, mpg)) + geom_point() + theme_journal("frontiers")
out <- file.path(tempdir(), "figure_1.tiff")
ggsave_journal(out, p, "frontiers")
unlink(out)
}
