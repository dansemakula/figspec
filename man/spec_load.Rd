% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry.R
\name{spec_load}
\alias{spec_load}
\title{Load specifications from a YAML registry}
\usage{
spec_load(path)
}
\arguments{
\item{path}{Path to a non-empty YAML registry file. The file may contain a
top-level \verb{journals:} list, like figspec's bundled registry, or be the list
of entries itself.}
}
\value{
A character vector containing the loaded ids, invisibly.
}
\description{
Reads a YAML registry maintained by a publication, project or organisation
and adds its specifications to the current R session. Each entry then works
with the same building, exporting and verification functions as a bundled
profile. \code{\link[=spec_list]{spec_list()}} marks loaded entries as user-supplied.
}
\details{
Keep the YAML file with the project or in a shared version-controlled
repository. When requirements change, edit the file and call
\code{spec_load()} again; entries with the same user-defined id are updated
for the current session. A loaded file cannot replace a profile bundled with
figspec.

Registry YAML is treated as data: YAML expression evaluation is disabled
regardless of the user's global \code{yaml.eval.expr} option. The complete file
is validated before any entry is added, so an invalid entry cannot leave a
partly updated session.
}
\examples{
# spec_load("my-journals.yaml")
}
\seealso{
\code{\link[=spec_register]{spec_register()}} to add one entry directly in R and
\code{\link[=registry_validate_file]{registry_validate_file()}} to check a file without loading it.
}
