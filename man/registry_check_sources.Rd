% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maintenance.R
\name{registry_check_sources}
\alias{registry_check_sources}
\title{Check whether registry source pages still respond}
\usage{
registry_check_sources(ids = NULL, timeout = 10)
}
\arguments{
\item{ids}{Character vector of registry ids to check. Defaults to all of
them.}

\item{timeout}{One positive number giving the seconds allowed for each
response.}
}
\value{
A data frame with one row per entry: \code{id}, \code{url}, \code{http} (the status
code, \code{NA} if nothing answered), \code{verdict}, and \code{final_url} where a
redirect led somewhere else. Ordered worst first.
}
\description{
Every entry names the page it was read from, and a page can be taken down
without anything in the registry changing. This asks each source URL whether
it still resolves.
}
\details{
A publisher that blocks robots is not a broken link. Most academic
publishers sit behind bot protection and answer a scripted request with
\code{403} while the page opens normally in a browser, so those are reported as
\emph{blocked} and are not failures. Only \code{404} and \code{410} are read as dead.

Run this during registry maintenance when an internet connection is
available. Offline package checks can use \code{\link[=registry_status]{registry_status()}} to review the
stored sources and dates.
}
\examples{
# Reaches the network, so it is not run here.
\dontrun{
registry_check_sources()
registry_check_sources("plos_one")
}
}
\seealso{
\code{\link[=registry_status]{registry_status()}} for how old an entry is, \code{\link[=registry_stale_entries]{registry_stale_entries()}} for
which are due a recheck.
}
