% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme.R
\name{figspec_linewidth}
\alias{figspec_linewidth}
\title{Line width that satisfies a journal's minimum}
\usage{
figspec_linewidth(journal)
}
\arguments{
\item{journal}{Registry id.}
}
\value{
A single numeric \code{linewidth}, or \code{NULL} when the journal states no
minimum.
}
\description{
Several publishers state a minimum line weight in points. ggplot2 measures
\code{linewidth} in millimetres, so this converts the journal's figure into the
number you pass to a geom. \code{\link[=theme_journal]{theme_journal()}} already applies it to axes,
ticks and gridlines, but geom line widths are set on the layer, not the
theme, so pass this to layers that draw lines.
}
\examples{
library(ggplot2)
lw <- figspec_linewidth("frontiers")
ggplot(ggplot2::economics, aes(date, unemploy)) +
  geom_line(linewidth = lw) +
  theme_journal("frontiers")
}
