% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{fit_journal}
\alias{fit_journal}
\title{Bring a journal's requirements into a plot as you build it}
\usage{
fit_journal(
  journal,
  colour = TRUE,
  shapes = TRUE,
  style = NULL,
  base_size = NULL,
  color = NULL
)
}
\arguments{
\item{journal}{Registry id, for example \code{"cell_press"}.}

\item{colour}{Whether to set the colour and fill scales. Turn this off to
keep a palette you have chosen yourself.}

\item{shapes}{Whether to set the shape scale.}

\item{style}{A house style registered with \code{\link[=register_house_style]{register_house_style()}}, applied
underneath the journal's requirements.}

\item{base_size}{Base type size in points, passed to \code{\link[=theme_journal]{theme_journal()}}.}

\item{color}{American spelling of \code{colour}. Takes precedence when given.
R's partial matching cannot cover this one, because \code{color} is not a
prefix of \code{colour} - the spellings diverge at the fifth letter.}
}
\value{
A list of ggplot2 components, to add to a plot with \code{+}.
}
\description{
Adds a journal's requirements to a plot the same way you would add a colour
scale, so the figure is built to specification from the start rather than
corrected afterwards. One line carries the journal's typography, its stated
line weights, its structural rules such as axis lines and tick marks, and
colours and shapes chosen to survive whatever that journal does to a figure
in production.
}
\details{
Add it last. A scale added after this one replaces the journal's, which is
occasionally what you want and usually not.

The palette follows the journal. Where a publisher reproduces figures in
black and white, \code{fit_journal()} reaches for cividis, which keeps its
colours apart in greyscale. Everywhere else it uses Okabe-Ito, built to stay
readable under the common forms of colour vision deficiency.

Line widths inside a geom are set on the layer rather than the theme, so
pass \code{\link[=figspec_linewidth]{figspec_linewidth()}} to any layer that draws lines.
}
\examples{
library(ggplot2)

ggplot(mtcars, aes(wt, mpg, colour = factor(cyl), shape = factor(cyl))) +
  geom_point() +
  fit_journal("cell_press")

# Keep your own palette, take everything else.
ggplot(mtcars, aes(wt, mpg, colour = factor(cyl))) +
  geom_point() +
  scale_colour_viridis_d() +
  fit_journal("plos_one", colour = FALSE)
}
