% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maintenance.R
\name{check_sources}
\alias{check_sources}
\title{Are the pages the registry cites still there}
\usage{
check_sources(ids = NULL, timeout = 10)
}
\arguments{
\item{ids}{Entries to check. Defaults to all of them.}

\item{timeout}{Seconds to wait for each response.}
}
\value{
A data frame with one row per entry: \code{id}, \code{url}, \code{http} (the status
code, \code{NA} if nothing answered), \code{verdict}, and \code{final_url} where a
redirect led somewhere else. Ordered worst first.
}
\description{
Every entry names the page it was read from, and a page can be taken down
without anything in the registry changing. This asks each source URL whether
it still resolves.
}
\details{
A publisher that blocks robots is not a broken link. Seven of the sources in
the registry answer \code{403} to a scripted request while the page opens fine in
a browser, so those are reported as \emph{blocked} and are not failures. Only
\code{404} and \code{410} are read as dead. The distinction is the whole point: a
checker that counted every refusal as a death would cry wolf seven times for
each real one.

This reaches the network, so it is for maintainers rather than for use
inside anything that has to run offline.
}
\examples{
# Reaches the network, so it is not run here.
\dontrun{
check_sources()
check_sources("plos_one")
}
}
\seealso{
\code{\link[=registry_status]{registry_status()}} for how old an entry is, \code{\link[=stale_entries]{stale_entries()}} for
which are due a recheck.
}
