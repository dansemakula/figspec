% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tag.R
\name{fig_tag_panels}
\alias{fig_tag_panels}
\title{Add and format labels for every panel}
\usage{
fig_tag_panels(
  plot,
  spec = NULL,
  level = NULL,
  open = "(",
  close = ")",
  strips = FALSE,
  x = -Inf,
  y = Inf,
  hjust = -0.6,
  vjust = 1.4,
  ...
)
}
\arguments{
\item{plot}{A faceted ggplot or a patchwork composition to label.}

\item{spec}{A registry id, a \code{figspec_spec}, or a named list containing
the panel-label requirements.}

\item{level}{Label vocabulary: \code{"A"}, \code{"a"}, \code{"1"}, \code{"I"} or \code{"i"}.
An explicit value overrides the specification; \code{\link[=fig_check]{fig_check()}} will report
a failure if the choice conflicts with a stated requirement.}

\item{open, close}{The characters placed before and after each label.
Parentheses are used by default.}

\item{strips}{Whether to retain facet-strip headings. This is ignored for a
patchwork composition.}

\item{x, y, hjust, vjust}{The position and alignment of labels inside each
faceted panel.}

\item{...}{Additional label settings passed to \code{\link[ggplot2:geom_text]{ggplot2::geom_text()}}, such
as \code{size}, \code{fontface}, \code{family}, or \code{colour}.}
}
\value{
The plot, labelled. \code{\link[=fig_check]{fig_check()}} recognises the result.
}
\description{
Adds panel labels in the style the journal asks for. Where a publisher
states one — capitals, lower case, numbers — that is what you get, so the
convention does not have to be looked up.
}
\details{
A composition made with patchwork is labelled through patchwork's own tags.
A faceted plot has no such machinery, so the labels are drawn inside the
panels and the facet strips are removed, which is the convention for
journals that treat facets as sub-figures. Keep the strips with
\code{strips = TRUE} if their content is doing work the labels do not replace.

When the specification gives no labelling rule, \code{level} supplies the
convention for the figure. If you leave it unset, figspec uses lower-case
letters and records that choice as a convention in the report.
}
\examples{
library(ggplot2)
p <- ggplot(ggplot2::mpg, aes(displ, hwy)) +
  geom_point() + facet_wrap(~drv)

# Cell Press asks for capitals; AGU asks for lower case.
fig_tag_panels(p, "cell_press")

# Or say it yourself, where no journal is involved.
fig_tag_panels(p, level = "a")
}
\seealso{
\code{\link[=fig_check]{fig_check()}}, which reports whether a figure's panels meet the
journal's labelling rule.
}
