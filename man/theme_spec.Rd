% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme.R
\name{theme_spec}
\alias{theme_spec}
\title{Apply typography requirements and a visual style}
\usage{
theme_spec(
  spec,
  base = NULL,
  style = NULL,
  base_size = NULL,
  base_family = NULL
)
}
\arguments{
\item{spec}{A registry id such as \code{"plos_one"}, a \code{figspec_spec}, or a
named list of requirements.}

\item{base}{A ggplot2 theme to build on. Defaults to \code{\link[ggplot2:theme_bw]{ggplot2::theme_bw()}}.}

\item{style}{A house style to apply: a name registered with
\code{\link[=style_register]{style_register()}}, a ggplot2 theme, or a function returning one.
Styles are applied underneath the journal's requirements and can never
override them.}

\item{base_size}{Base type size in points. Defaults to the journal's stated
minimum, or 9 pt when the journal states none.}

\item{base_family}{Font family. Defaults to the graphics device's own font.
The journal's named font is \emph{not} forced here, because a family the
current device cannot resolve makes the plot fail to render at all.
\code{\link[=fig_save]{fig_save()}} applies the journal's font at save time, where the
device is known. Pass a family explicitly to override.}
}
\value{
A ggplot2 theme object.
}
\description{
\code{theme_spec()} takes any base theme and applies the typographic constraints
in a specification on top of it, rather than imposing a look of its own.
The specification may come from a publisher, a journal or a project. Every
text element is set to at least its minimum size, so nothing falls below the
floor because of a relative sizing rule in the base theme.
}
\details{
This only holds if the figure is saved at the journal's stated width, since
type size is fixed in points but a figure scaled down after the fact takes
its text with it. \code{\link[=fig_save]{fig_save()}} saves at the right width for you.
}
\examples{
library(ggplot2)
p <- ggplot(ggplot2::economics, aes(date, unemploy)) +
  geom_point() +
  theme_spec("plos_one")

# Project typography can be supplied directly.
report_spec <- list(name = "Annual report", font_min_pt = 10)
ggplot(ggplot2::economics, aes(date, unemploy)) +
  geom_line() +
  theme_spec(report_spec)
}
\seealso{
\code{\link[=fig_apply_spec]{fig_apply_spec()}} to apply typography, colour and shape together, and
\code{\link[=style_register]{style_register()}} to reuse an organisation or project style.
}
