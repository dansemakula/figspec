% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry.R
\name{spec_register}
\alias{spec_register}
\title{Register a publication specification for this session}
\usage{
spec_register(
  id,
  name,
  source_url,
  verified_on,
  requirements = list(),
  house_style = NULL,
  ...
)
}
\arguments{
\item{id}{A short, unique identifier beginning with a lower-case letter and
containing only lower-case letters, numbers and underscores. This is the
value supplied to functions such as \code{fig_save(spec = "my_journal")}.}

\item{name}{The publication name shown in reports and registry listings.}

\item{source_url}{Where the requirements came from: an HTTP(S) page, a
\verb{file:} URI or an \verb{internal:} identifier.}

\item{verified_on}{Date the source was last checked, as \code{"YYYY-MM-DD"} or a
\code{Date}. Future and invalid dates are rejected.}

\item{requirements}{A named list containing only requirements stated by the
source, such as \code{columns}, \code{dpi_min}, \code{formats}, \code{font_min_pt} or
\code{min_line_pt}. Values are validated before the entry is registered.}

\item{house_style}{Optional named visual preferences, such as
\code{list(palette = c("#1B4965", "#CA6702"))}. Requirements do not belong
here and are rejected if included.}

\item{...}{Optional registry information such as \code{publisher}, \code{disciplines},
\code{publication_stage}, \code{notes}, \code{tables}, \code{media} or
\code{graphical_abstract}.}
}
\value{
The registered specification, invisibly.
}
\description{
Adds a specification that is not bundled with figspec, such as requirements
for an individual journal, conference or other publication. The entry lasts
for the current R session and can immediately be used with \code{\link[=fig_save]{fig_save()}},
\code{\link[=fig_check]{fig_check()}}, \code{\link[=fig_apply_spec]{fig_apply_spec()}} and the other specification-aware functions.
\code{\link[=spec_list]{spec_list()}} marks it as user-supplied so it remains distinct from profiles
maintained by figspec.
}
\details{
Every registered entry needs a source and a date. Use the public guidance
page when one exists. For a private manual or internal decision, use an
\verb{internal:} identifier such as \code{"internal:figure-handbook-v3"}.
}
\examples{
spec_register(
  id = "lab_report",
  name = "Our lab report format",
  source_url = "internal:handbook-v3",
  verified_on = "2026-08-22",
  requirements = list(columns = list(single = 100, double = 170),
                      font_min_pt = 9, formats = list("pdf"))
)
fig_width("lab_report", "double")
}
\seealso{
\code{\link[=spec_save]{spec_save()}} to write a reusable YAML registry, \code{\link[=spec_load]{spec_load()}} to
load reusable entries and \code{\link[=registry_entry_template]{registry_entry_template()}} to create a registry
template.
}
