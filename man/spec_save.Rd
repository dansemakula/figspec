% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry.R
\name{spec_save}
\alias{spec_save}
\title{Save a specification for reuse}
\usage{
spec_save(
  spec,
  path,
  id = NULL,
  source_url = NULL,
  verified_on = NULL,
  overwrite = FALSE
)
}
\arguments{
\item{spec}{A registry id, a \code{figspec_spec}, or a named list containing at
least a non-empty \code{name} and any figure, table, media or graphical-abstract
requirements to save.}

\item{path}{Destination \code{.yaml} or \code{.yml} file. If it already contains a
valid figspec registry, a new \code{id} is appended without removing the other
entries.}

\item{id}{A short identifier beginning with a lower-case letter and
containing only lower-case letters, numbers and underscores. It may be
omitted when \code{spec} already contains an \code{id}.}

\item{source_url}{Where the requirements came from: an HTTP(S) page, a
\verb{file:} URI or an \verb{internal:} identifier. When omitted, an existing value
in \code{spec} is kept; otherwise \verb{internal:<id>} is recorded.}

\item{verified_on}{Date the source or internal requirements were last
checked, as \code{"YYYY-MM-DD"} or a \code{Date}. When omitted, an existing value in
\code{spec} is kept; otherwise today's date is recorded.}

\item{overwrite}{Whether an entry with the same \code{id} may be replaced.
Other entries in the file are always preserved.}
}
\value{
\code{path}, invisibly.
}
\description{
Writes a publication, project or organisational specification to a safe,
reusable YAML registry file. A new specification is appended when the file
already contains other entries. Replacing an entry with the same \code{id}
requires \code{overwrite = TRUE}.
}
\details{
The complete registry is validated in a temporary file before the requested
path is changed. If writing or validation fails, an existing file is left
unchanged. The resulting file can be kept with a project, shared under
version control and loaded in any R session with \code{\link[=spec_load]{spec_load()}}.

A specification created directly in R may not contain provenance fields. In
that case figspec records \verb{internal:<id>} as its source and today's date as
the date the internal requirements were recorded. Supply \code{source_url} and
\code{verified_on} when the requirements came from a published or separately
maintained source.
}
\examples{
report_spec <- list(
  name = "Research unit report",
  columns = list(full = 160),
  dpi_min = 300,
  formats = c("png", "pdf")
)
registry_file <- tempfile(fileext = ".yml")
spec_save(report_spec, registry_file, id = "research_unit_report")
spec_load(registry_file)
fig_width("research_unit_report", "full")
unlink(registry_file)
}
\seealso{
\code{\link[=spec_load]{spec_load()}}, \code{\link[=spec_register]{spec_register()}}, \code{\link[=registry_validate_file]{registry_validate_file()}}
}
