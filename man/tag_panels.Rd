% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tag.R
\name{tag_panels}
\alias{tag_panels}
\title{Label the panels of a figure}
\usage{
tag_panels(
  plot,
  journal = NULL,
  level = NULL,
  open = "(",
  close = ")",
  strips = FALSE,
  x = -Inf,
  y = Inf,
  hjust = -0.6,
  vjust = 1.4,
  ...
)
}
\arguments{
\item{plot}{A ggplot, faceted or not, or a patchwork composition.}

\item{journal}{Registry id, or a specification. Supplies the label style.}

\item{level}{Label vocabulary: \code{"A"}, \code{"a"}, \code{"1"}, \code{"I"} or \code{"i"}.
Overrides the journal, which is what you want when a co-author has asked
for something the publisher does not mention.}

\item{open, close}{Characters around the label. Journals rarely state these;
\code{"("} and \code{")"} are common in print.}

\item{strips}{Whether to keep facet strips. Ignored for a composition.}

\item{x, y, hjust, vjust}{Where the label sits inside its panel.}

\item{...}{Passed to \code{\link[ggplot2:geom_text]{ggplot2::geom_text()}}, for size, face or family.}
}
\value{
The plot, labelled. \code{\link[=fig_check]{fig_check()}} recognises the result.
}
\description{
Adds panel labels in the style the journal asks for. Where a publisher
states one — capitals, lower case, numbers — that is what you get, so the
convention does not have to be looked up.
}
\details{
A composition made with patchwork is labelled through patchwork's own tags.
A faceted plot has no such machinery, so the labels are drawn inside the
panels and the facet strips are removed, which is the convention for
journals that treat facets as sub-figures. Keep the strips with
\code{strips = TRUE} if their content is doing work the labels do not replace.

Nothing here is invented. Where a journal states no labelling rule, the
\code{level} you give applies, and if you give none the default is lower case —
a convention, and reported as one rather than as a requirement.
}
\examples{
library(ggplot2)
p <- ggplot(mtcars, aes(wt, mpg)) + geom_point() + facet_wrap(~cyl)

# Cell Press asks for capitals; AGU asks for lower case.
tag_panels(p, "cell_press")

# Or say it yourself, where no journal is involved.
tag_panels(p, level = "a")
}
\seealso{
\code{\link[=fig_check]{fig_check()}}, which reports whether a figure's panels meet the
journal's labelling rule.
}
