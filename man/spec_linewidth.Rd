% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme.R
\name{spec_linewidth}
\alias{spec_linewidth}
\title{Use line widths that meet a specification}
\usage{
spec_linewidth(spec)
}
\arguments{
\item{spec}{A registry id, a \code{figspec_spec}, or a named list containing
the required minimum line width.}
}
\value{
A single numeric \code{linewidth}, or \code{NULL} when the specification states no
minimum.
}
\description{
Some specifications state a minimum line weight in points, while ggplot2
uses a different scale for its \code{linewidth} argument. This function converts
the recorded requirement into the value you pass to a line-drawing geom.
\code{\link[=theme_spec]{theme_spec()}} applies the requirement to axes, ticks and gridlines, but
lines drawn by data layers must be set directly.
}
\examples{
library(ggplot2)
lw <- spec_linewidth("frontiers")
ggplot(ggplot2::economics, aes(date, unemploy)) +
  geom_line(linewidth = lw) +
  theme_spec("frontiers")
}
