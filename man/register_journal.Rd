% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry.R
\name{register_journal}
\alias{register_journal}
\title{Add your own journal to the registry}
\usage{
register_journal(
  id,
  name,
  source_url,
  verified_on,
  requirements = list(),
  house_style = NULL,
  ...
)
}
\arguments{
\item{id}{Short identifier, used everywhere a journal is named.}

\item{name}{Human-readable journal name.}

\item{source_url}{Where the requirements came from.}

\item{verified_on}{Date the source was read, as \code{"YYYY-MM-DD"}.}

\item{requirements}{Named list of stated requirements. See the shipped
registry for the field names.}

\item{house_style}{Optional named list of purely visual preferences. May
not contain requirement fields.}

\item{...}{Further entry fields such as \code{publisher} or \code{disciplines}.}
}
\value{
The registered specification, invisibly.
}
\description{
Registers a journal for the current session, for a journal figspec does not
ship yet or for an internal report format of your own. Registered journals
behave exactly like shipped ones, but \code{\link[=journals]{journals()}} marks them as
user-supplied so it stays obvious which entries figspec stands behind.
}
\details{
Provenance is required here too. If the requirements came from you rather
than a publisher, say so in \code{source_url}.
}
\examples{
register_journal(
  id = "lab_report",
  name = "Our lab report format",
  source_url = "internal handbook v3",
  verified_on = "2026-08-22",
  requirements = list(columns = list(single = 100, double = 170),
                      font_min_pt = 9, formats = list("pdf"))
)
fig_width("lab_report", "double")
}
