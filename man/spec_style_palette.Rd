% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/palette.R
\name{spec_style_palette}
\alias{spec_style_palette}
\title{Retrieve a recorded house-style palette}
\usage{
spec_style_palette(spec)
}
\arguments{
\item{spec}{The specification whose house-style palette should be
retrieved. Supply a registry id, a \code{figspec_spec}, or a named list. It may
describe a publication, project or organisation.}
}
\value{
A character vector of recorded R colours, preserving any names, or
\code{NULL} when the specification contains no house-style palette.
}
\description{
Retrieves the optional palette stored in the \code{house_style} section of a
registry or user-supplied specification. This can represent the visual
identity of a publication, project or organisation.
}
\details{
A house-style palette is a design preference, not a requirement. It is never
graded by \code{\link[=fig_check]{fig_check()}}, and using it does not by itself make a figure comply
with a specification. When no palette is recorded, figspec reports that fact
and returns \code{NULL} rather than inventing colours.
}
\examples{
spec_style_palette("plos_one")
}
