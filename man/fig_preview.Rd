% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preview.R
\name{fig_preview}
\alias{fig_preview}
\title{Preview a figure at its intended output size}
\usage{
fig_preview(
  plot = ggplot2::last_plot(),
  spec,
  column = "single",
  height = NULL,
  units = c("mm", "cm", "in")
)
}
\arguments{
\item{plot}{Figure to preview. Accepts the same live figure inputs as
\code{\link[=fig_save]{fig_save()}} and defaults to the last ggplot2 plot displayed.}

\item{spec}{A registry id, a \code{figspec_spec}, or a named list containing
the available output widths.}

\item{column}{Which column width to preview at.}

\item{height}{Height. Defaults to three quarters of the width.}

\item{units}{Units for \code{height}.}
}
\value{
The plot, invisibly.
}
\description{
The most common way a compliant figure becomes non-compliant is being
designed on a laptop at roughly 7 by 5 inches and submitted into an 85 mm
column. Everything in it is then reduced by more than half. This opens a
device at the journal's real column width, so what you are looking at while
you iterate is the size a reader will see.
}
\examples{
library(ggplot2)
p <- ggplot(ggplot2::mpg, aes(displ, hwy, colour = class)) + geom_point()
if (interactive()) fig_preview(p, "cell_press", "single")
p
}
\seealso{
\code{\link[=fig_save]{fig_save()}} to write the figure at that size and \code{\link[=fig_width]{fig_width()}} to
retrieve a recorded width without opening a preview.
}
