% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/knitr.R
\name{figspec_knitr_options}
\alias{figspec_knitr_options}
\title{Create figure settings for R Markdown or Quarto}
\usage{
figspec_knitr_options(
  spec,
  column = NULL,
  width = NULL,
  height = NULL,
  units = c("mm", "cm", "in"),
  art_type = c("auto", "colour", "bw", "line", "combination")
)
}
\arguments{
\item{spec}{The specification to use: a registry id such as \code{"plos_one"},
a \code{figspec_spec}, or a named list of requirements.}

\item{column}{Which named width in the specification to use. Leave it \code{NULL}
when supplying \code{width} explicitly.}

\item{width}{Explicit figure width for specifications that do not publish
named columns.}

\item{height}{Figure height. Defaults to three quarters of the width when no
height is recorded; supply an explicit value when the specification or
layout requires one.}

\item{units}{Units for \code{width} and \code{height}.}

\item{art_type}{Resolution category. With no plot available, \code{"auto"}
conservatively uses the strictest rule the journal states.}
}
\value{
A named list suitable for \code{knitr::opts_chunk$set()}.
}
\description{
Figures created inside an R Markdown or Quarto code chunk do not pass through
\code{\link[=fig_save]{fig_save()}}. This function translates a publication, project or
organisational specification into the width, height, resolution and graphics
device settings understood by knitr.
}
\examples{
figspec_knitr_options("frontiers", "single")

# A report format maintained by your own team.
report_spec <- list(
  name = "Landscape report figure",
  columns = list(full = 180),
  formats = "png",
  dpi_min = 300
)
figspec_knitr_options(report_spec, "full", height = 100, units = "mm")

# In a setup chunk:
# do.call(knitr::opts_chunk$set, figspec_knitr_options("frontiers"))
}
