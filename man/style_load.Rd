% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/style.R
\name{style_load}
\alias{style_load}
\title{Load saved visual styles}
\usage{
style_load(path, allow_functions = FALSE)
}
\arguments{
\item{path}{The RDS file previously created by \code{\link[=style_save]{style_save()}}.}

\item{allow_functions}{Whether to permit stored theme functions. Defaults to
\code{FALSE}.}
}
\value{
The names of the loaded styles, invisibly.
}
\description{
Restores styles previously written by \code{\link[=style_save]{style_save()}} and adds them to
the styles already registered in the current R session. An RDS file is a
trusted-input format; do not load one received from an untrusted person or
website.
}
\details{
Theme functions contain executable R code. They are refused by default even
when the rest of the file is valid. Set \code{allow_functions = TRUE} only for a
file you created and control.
}
\examples{
library(ggplot2)
style_register("mylab", theme_minimal())
f <- tempfile(fileext = ".rds")
style_save(f)
style_remove("mylab")
style_load(f)
unlink(f)
}
