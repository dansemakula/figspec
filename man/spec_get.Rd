% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry.R
\name{spec_get}
\alias{spec_get}
\title{Retrieve or create a specification}
\usage{
spec_get(spec)
}
\arguments{
\item{spec}{The specification to use: a registry id such as
\code{"plos_one"}, an existing \code{figspec_spec}, or a named list containing at
least a non-empty \code{name}. Use \code{\link[=spec_list]{spec_list()}} to browse the available registry
ids. The specification does not have to describe a journal.}
}
\value{
An object of class \code{figspec_spec}. An existing specification object
is returned unchanged.
}
\description{
Retrieves a bundled publisher or journal profile by its registry id, or
converts a named list maintained by your project or organisation into a
\code{figspec_spec}. The resulting object can be passed to \code{\link[=fig_check]{fig_check()}},
\code{\link[=fig_save]{fig_save()}} and the other specification-aware functions.
}
\examples{
spec_get("frontiers")

report_spec <- spec_get(list(
  name = "Quarterly research report",
  columns = list(full = 160),
  formats = c("png", "pdf"),
  dpi_min = 300,
  font_min_pt = 9
))
report_spec
}
